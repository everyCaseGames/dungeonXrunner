DungeonXrunner wurde komplett in assembly geschrieben und mit NESASM3 kompiliert.

Windows-User können sich mit der make.bat eine .nes Datei erstellen lassen.

Die anderen Assembly-Dateien enthalten eingebundene Musik.

Die .nes Datei kann auf einem beliebigen Emulator gestartet werden (zuletzt auf FCEUX getestet).